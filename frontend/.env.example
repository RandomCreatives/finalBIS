# Base URL of the backend API (no trailing slash, no /api suffix).
# Local development:
REACT_APP_API_URL=http://localhost:5000

# Production example:
# REACT_APP_API_URL=https://your-backend.vercel.app
